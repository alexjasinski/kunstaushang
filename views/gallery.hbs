
<h1>GALLERY</h1>
<div class="gallery">
  {{#each images}}
    <div>
      <img src="{{urls.small}}" alt="{{this.alt_description}}" />
    <p>{{this.alt_description}}</p>
    </div>
  {{/each}}
</div>
<div class="container">
  {{> searchBar}}


</div>